<section>
    <div  class="card">
        <div class="card-header" style=" text-align:center; background:#0d6efd; color:#FFFFFF; ">
            <h5 style="font-size: 15px;">{{clinic.name}}</h5>
            <span style="text-align:center; background:#0d6efd; color:#FFFFFF;">Col. {{clinic.neighborhood}}</span>
        </div>
        <div class="card-body">
            @if(!clinic.ailments.length)
                <span style="text-align:center; color:#000000;">No se encuentran datos para el año de {{year.year}}</span>
            @else
                <table class="table table-bordered" style="margin-left:auto; margin-right:auto; padding-top:8px; padding-left:55px; padding-right:55px;">
                    <thead>
                        <tr style="background-color:#white; color:#0d6efd;">
                            <th style="text-align:center;">Padecimiento</th>
                            <th style="text-align:center;">No. Casos</th>
                        </tr>
                    </thead>
                    <tbody>
                        @each(ailment in clinic.ailments)
                        <tr>
                            <td style="text-align:center;">{{ailment.name}}</td>
                            <td style="text-align:center;">{{ailment.$extras.pivot_total}}</td>
                        </tr>
                        @endeach
                    </tbody>
                    <tfoot>
                    <tr>
                    <td colspan="3" style="text-align:center;">Datos del año {{year.year}}</td>
                    </tr>
                    </tfoot>
                </table>

                
            @endif
            <br>
            <div class='d-grid gap-2' style="text-align:center;">
                <a class='btn btn-primary' href="{{route('historic_clinic',[clinic.id])}}">Histórico</a>
            </div>
        </div>
    </div>
</section>

<!--
-->